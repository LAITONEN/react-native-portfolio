/*import React from 'react';
import { DrawerNavigator } from 'react-navigation';
import Icon from 'react-native-vector-icons/Entypo';

// destructuring because I am importing "consts", not "defaults"
import { DictionariesTabBar } from './DictionariesNavigator';
import { GamesTabBar } from './GamesNavigator';
import { SettingsStack } from './SettingsNavigator'; 



// CONFIGS

const routeConfig = {
      dictionaries: { screen: DictionariesTabBar, navigationOptions: {
							drawerIcon: <Icon name="book" size={25} />,
							drawerLabel: 'Dictionaries' } },
      games: { screen: GamesTabBar },
      settings: { screen: SettingsStack } };

const navigatorConfig = {};

export const MainDrawer = DrawerNavigator(routeConfig, navigatorConfig);*/